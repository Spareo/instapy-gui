FROM node:10-alpine

ARG APP_MONGO_URL
ARG APP_JWT_SECRET=""

ENV MONGO_URL="${APP_MONGO_URL}"
ENV JWT_SECRET=${APP_JWT_SECRET}

WORKDIR /usr/instapy

COPY . .

RUN npm install

EXPOSE 80

CMD ["npm", "start"]